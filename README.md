# AI Project Structure Generator

A simple and beginner-friendly Python script to automatically create a clean folder structure for AI / Machine Learning projects.

This tool helps you start new projects with an organized setup instead of manually creating folders every time.

---

## 📁 Folder Structure Created

The script generates the following directories:

- `data/` – Store raw and processed datasets  
- `model/` – Save trained machine learning models  
- `scripts/` – Python scripts and utilities  
- `notebook/` – Jupyter notebooks for experimentation  
- `output/` – Results, logs, and generated outputs  

---

## 🚀 How to Use

### 1. Clone the repository
```bash
git clone https://github.com/anupxsharma/ai-project-structure-generator.git
